exports.up = async (client) => {
  await client`create table items (
		id integer PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
		name varchar(40) NOT NULL,
		price integer NOT NULL
	)`;
};

exports.down = async (client) => {
  await client`
    DROP TABLE items
  `;
};
